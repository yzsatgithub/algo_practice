#include <iostream>
#include <string>
#include <cstring>
#include <algorithm>
#include <sstream>
#include <cstdio>
#include <cstdlib>
#include <vector>

using namespace std;

/*
 * 题目：
 * 记不住了。
 * 大致是，构建一个多叉树，根节点编号0， 如果父节点编号为i，则子节点从左到右以此编号i0, i1, i2, ...。  需要所有节点编号按照层序遍历顺序拼接起来
 * 得到的字符串。
 *
 *
 *


int main()
{
	return 0;
}
